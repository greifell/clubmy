name: Atualizar Ofertas JSON

on:
  schedule:
    - cron: '0 9,18 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  collect-offers-json:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          ref: main

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install dependencies
        run: npm ci

      - name: Install Python downloader dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install requests beautifulsoup4 playwright
          python -m playwright install --with-deps chromium

      - name: Download MM Rosso catalogs
        run: python scripts/download_mmrosso_catalogs.py

      - name: Collect offers
        run: npm run collect:offers

      - name: Commit updated JSON
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add -A input_catalogs
          git add public/data/offers-vtex.json frontend/public/data/offers-vtex.json

          if [ -f archive/expired-offers.json ]; then
            git add archive/expired-offers.json
          fi

          if git diff --cached --quiet; then
            echo "Nenhuma alteracao de ofertas para publicar."
            exit 0
          fi

          git commit -m "Atualiza ofertas de supermercados"
          git push origin HEAD:main
